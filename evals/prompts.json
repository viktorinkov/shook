[
 {"id":"explain-01","cat":"explain","prompt":"Explain to a junior developer why this Python test fails:\n\n```python\ndef test_upload():\n    client = Mock()\n    client.upload.return_value = None\n    result = service.sync(client)\n    assert result.ok\n```\n`service.sync` calls `client.upload()` and reads `.status` from what it returns."},
 {"id":"explain-02","cat":"explain","prompt":"Explain what a race condition is and how a mutex prevents it, for a README section aimed at contributors new to concurrency."},
 {"id":"explain-03","cat":"explain","prompt":"Explain the difference between `git merge` and `git rebase` to a teammate who asked which one to use for a feature branch."},
 {"id":"explain-04","cat":"explain","prompt":"Explain why `SELECT * FROM orders WHERE created_at > NOW() - INTERVAL '1 day'` can be slow on a table with 50 million rows, and what index helps."},
 {"id":"explain-05","cat":"explain","prompt":"Explain how JWT authentication works between a browser and an API, in a short section for our internal wiki."},
 {"id":"explain-06","cat":"explain","prompt":"Explain what this shell line does and when it is dangerous: `find . -name '*.log' -mtime +7 -exec rm {} \\;`"},
 {"id":"explain-07","cat":"explain","prompt":"Explain to a product manager why adding a database index is not free."},
 {"id":"explain-08","cat":"explain","prompt":"Explain the N+1 query problem with a short example in an ORM, and how to fix it."},
 {"id":"explain-09","cat":"explain","prompt":"Explain why floating point makes `0.1 + 0.2 != 0.3` and what to use for money."},
 {"id":"explain-10","cat":"explain","prompt":"Explain the purpose of a `.gitignore` file and what typically belongs in it, for a getting-started guide."},
 {"id":"review-01","cat":"review","prompt":"Write a code review comment for this function. Be specific about the problem and the fix.\n\n```js\nasync function getUser(id) {\n  const res = await fetch('/api/users/' + id);\n  return res.json();\n}\n```"},
 {"id":"review-02","cat":"review","prompt":"Review this SQL migration and write your feedback as a PR comment:\n\n```sql\nALTER TABLE users ADD COLUMN email VARCHAR(255);\nUPDATE users SET email = username || '@example.com';\nALTER TABLE users ALTER COLUMN email SET NOT NULL;\n```"},
 {"id":"review-03","cat":"review","prompt":"Write a review comment for a PR that adds a 400-line utility file with no tests."},
 {"id":"review-04","cat":"review","prompt":"Review this Python snippet and write a PR comment:\n\n```python\ntry:\n    data = json.loads(payload)\nexcept:\n    data = {}\n```"},
 {"id":"review-05","cat":"review","prompt":"Write a PR comment that asks the author to split a pull request that changes both the payment logic and the CSS theme."},
 {"id":"review-06","cat":"review","prompt":"Review this Dockerfile line and explain the problem in a PR comment: `RUN apt-get update && apt-get install -y curl` followed on a later line by `RUN apt-get install -y git`."},
 {"id":"review-07","cat":"review","prompt":"Write a PR review summary for a change that adds retries to an HTTP client but retries on every status code, including 400."},
 {"id":"review-08","cat":"review","prompt":"Write a review comment on a function named `processData` that takes six boolean parameters."},
 {"id":"review-09","cat":"review","prompt":"Review this Go code in a PR comment:\n\n```go\nfor _, item := range items {\n    go func() {\n        process(item)\n    }()\n}\n```"},
 {"id":"review-10","cat":"review","prompt":"Write a PR comment for a commit that hardcodes an API key in `config.py`."},
 {"id":"readme-01","cat":"docs","prompt":"Write the Installation section of a README for a CLI tool called `logtail` that is installed with `pip install logtail` and needs Python 3.10 or later."},
 {"id":"readme-02","cat":"docs","prompt":"Write a Getting Started section for a Node library `quickcache` that wraps Redis. Cover install, connecting, and one example of `get`/`set`."},
 {"id":"readme-03","cat":"docs","prompt":"Write the Configuration section of a README. The tool reads `config.toml` with keys `port` (default 8080), `log_level` (default info), and `workers` (default 4)."},
 {"id":"readme-04","cat":"docs","prompt":"Write a Troubleshooting section for a desktop app that sometimes fails to start with `EADDRINUSE`."},
 {"id":"readme-05","cat":"docs","prompt":"Write a short Contributing section: fork, branch, run `make test`, open a PR, and one reviewer must approve."},
 {"id":"readme-06","cat":"docs","prompt":"Write a README intro paragraph for `sqlpipe`, a tool that copies Postgres tables to S3 as Parquet files on a schedule."},
 {"id":"readme-07","cat":"docs","prompt":"Document the environment variables for a service: `DATABASE_URL` (required), `REDIS_URL` (optional, enables caching), `LOG_JSON` (default false)."},
 {"id":"readme-08","cat":"docs","prompt":"Write an API guide section for `POST /v1/invoices` that takes `customer_id`, `amount_cents`, and an optional `due_date`, and returns 201 with the invoice id."},
 {"id":"readme-09","cat":"docs","prompt":"Write the Upgrade section of a README for moving from version 1 to version 2, where the config file moved from `~/.tool.json` to `~/.config/tool/config.json`."},
 {"id":"readme-10","cat":"docs","prompt":"Write a Security section for a README: how to report a vulnerability by email, and the 90-day disclosure window."},
 {"id":"error-01","cat":"error","prompt":"Write the user-facing error message shown when a file upload exceeds the 25 MB limit."},
 {"id":"error-02","cat":"error","prompt":"Write the error message a CLI prints when the config file `~/.tool/config.toml` has a syntax error on line 12."},
 {"id":"error-03","cat":"error","prompt":"Write the error message for a failed database connection where the password for user `app` was rejected."},
 {"id":"error-04","cat":"error","prompt":"Write the message shown when a user tries to delete a project that still has active deployments."},
 {"id":"error-05","cat":"error","prompt":"Write the error text for an expired session that requires the user to log in again."},
 {"id":"commit-01","cat":"commit","prompt":"Write a commit message for a change that fixes a bug where the retry loop ignored the `max_attempts` setting and retried forever."},
 {"id":"commit-02","cat":"commit","prompt":"Write a commit message for adding a `--dry-run` flag to the `deploy` command."},
 {"id":"commit-03","cat":"commit","prompt":"Write a commit message for a change that replaces `moment.js` with the native `Intl.DateTimeFormat` and removes the dependency."},
 {"id":"commit-04","cat":"commit","prompt":"Write a commit message for a migration that adds an index on `orders(customer_id, created_at)`."},
 {"id":"commit-05","cat":"commit","prompt":"Write a commit message for a change that makes the health check endpoint return 503 while the cache warms up."},
 {"id":"incident-01","cat":"incident","prompt":"Write a short incident report. Between 14:02 and 14:31 UTC, 12% of API requests returned 500. Cause: a deploy at 14:00 removed the cache warmup step. Fix: revert at 14:27."},
 {"id":"incident-02","cat":"incident","prompt":"Write an incident summary for customers: the login service was unavailable for 18 minutes because a certificate expired. Renewal is now automated."},
 {"id":"incident-03","cat":"incident","prompt":"Write a postmortem section titled What went wrong: a cron job ran twice because two instances of the scheduler started after a failover."},
 {"id":"incident-04","cat":"incident","prompt":"Write a status page update: search results are delayed by up to 10 minutes while we reindex; no data is lost."},
 {"id":"incident-05","cat":"incident","prompt":"Write the Action items section of a postmortem for an outage caused by a disk that filled up with logs."},
 {"id":"release-01","cat":"release","prompt":"Write release notes for version 2.4.0: adds dark mode, fixes a crash on empty search, and drops support for Node 16."},
 {"id":"release-02","cat":"release","prompt":"Write release notes for a breaking change: the `--output` flag now defaults to JSON instead of plain text."},
 {"id":"runbook-01","cat":"runbook","prompt":"Write a runbook procedure to rotate the database password for the `app` user with zero downtime. The app reads the password from an environment variable and supports SIGHUP reload."},
 {"id":"runbook-02","cat":"runbook","prompt":"Write a runbook step list for restarting the `worker` service on a host, checking that the queue drains, and what to do if it does not."},
 {"id":"design-01","cat":"design","prompt":"Compare using a message queue versus direct HTTP calls between two internal services, and recommend one for an order-processing pipeline."}
]
